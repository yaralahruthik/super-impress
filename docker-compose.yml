# NOTE: This compose file is for local development of the backend + frontend only.
# In production, only the backend and database (postgres/redis) are deployed via Docker.
# UI sites (frontend, marketing-site, docs) are static builds deployed to platforms like Netlify.

services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-super_impress}
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data/pgdata
    networks:
      - app

  redis:
    image: redis:8-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - app
    command: redis-server --appendonly yes

  backend:
    build:
      context: ./backend
    image: super-impress-backend
    env_file:
      - backend/.env
      - backend/.env.docker
    ports:
      - "3000:3000"
    depends_on:
      - postgres
      - redis
    networks:
      - app

  frontend:
    build:
      context: ./frontend
    image: super-impress-frontend
    env_file:
      - frontend/.env
      - frontend/.env.docker
    depends_on:
      - backend
    ports:
      - "5173:5173"
    networks:
      - app

volumes:
  postgres-data:
  redis-data:

networks:
  app:
    driver: bridge
