/**
 * Generated by orval v8.0.2 🍺
 * Do not edit manually.
 * Super Impress
 * OpenAPI spec version: 0.1.0
 */
import * as zod from 'zod';

/**
 * @summary Register User
 */
export const registerUserBodyPasswordMin = 8;
export const registerUserBodyPasswordMax = 15;

export const RegisterUserBody = zod
	.object({
		email: zod.string().email(),
		password: zod
			.string()
			.min(registerUserBodyPasswordMin)
			.max(registerUserBodyPasswordMax)
			.describe('Must contain uppercase, lowercase, digit, and special character')
	})
	.describe('Schema for creating a new user.');

export const RegisterUserResponse = zod
	.object({
		email: zod.string().email(),
		id: zod.number(),
		email_verified: zod.boolean()
	})
	.describe('Schema for user public data (excludes password).');

/**
 * @summary Login User
 */
export const LoginUserResponse = zod
	.object({
		access_token: zod.string(),
		token_type: zod.string()
	})
	.describe('JWT token response schema.');

/**
 * @summary Read Current User
 */
export const ReadCurrentUserResponse = zod
	.object({
		email: zod.string().email(),
		id: zod.number(),
		email_verified: zod.boolean()
	})
	.describe('Schema for user public data (excludes password).');

/**
 * @summary Change Password
 */
export const changePasswordBodyNewPasswordMin = 8;
export const changePasswordBodyNewPasswordMax = 15;

export const ChangePasswordBody = zod
	.object({
		old_password: zod.string(),
		new_password: zod
			.string()
			.min(changePasswordBodyNewPasswordMin)
			.max(changePasswordBodyNewPasswordMax)
			.describe('Must contain uppercase, lowercase, digit, and special character')
	})
	.describe("Schema for changing a user's password.");

export const ChangePasswordResponse = zod.unknown();

/**
 * Delete the current user's account and all associated data.
 * @summary Delete Current User
 */
export const DeleteCurrentUserResponse = zod.unknown();
