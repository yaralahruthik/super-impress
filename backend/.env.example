# ============================================
# Database Configuration
# ============================================
# Local development (direct connection)
DATABASE_URL=postgresql+psycopg://{username}:{password}@localhost:5432/{database_name}

# Docker development (use service name 'postgres' as host)
# DATABASE_URL=postgresql+psycopg://{username}:{password}@postgres:5432/{database_name}

# ============================================
# JWT Authentication Configuration
# ============================================
SECRET_KEY=your-secret-key-here-change-this-in-production  # Generate with: openssl rand -hex 32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_MINUTES=10080  # 7 days

# ============================================
# Google OAuth Configuration
# ============================================
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# OAuth callback URL (backend endpoint where Google redirects after auth)
GOOGLE_REDIRECT_URI=http://localhost:8000/google/callback

# ============================================
# Frontend Configuration
# ============================================
FRONTEND_URL=http://localhost:5173

# ============================================
# Cookie Configuration
# ============================================
# Uncomment and configure for production
# COOKIE_DOMAIN=.yourdomain.com  # Use leading dot for subdomains
# COOKIE_SECURE=true  # MUST be true in production with HTTPS

# Development defaults (if not set):
# - COOKIE_DOMAIN=None (works for localhost)
# - COOKIE_SECURE=false
