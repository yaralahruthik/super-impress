repos:
  # Backend hooks (Biome)
  - repo: local
    hooks:
      - id: biome-check
        name: biome check
        entry: bunx biome check --write --no-errors-on-unmatched --files-ignore-unknown=true --colors=off
        language: system
        types: [text]
        files: ^backend/

  # Frontend hooks (ultracite)
  - repo: local
    hooks:
      - id: ultracite-frontend
        name: ultracite check (frontend)
        entry: bun --cwd frontend run check
        language: system
        pass_filenames: false
        files: ^frontend/
        types: [file]
        types_or: [javascript, jsx, ts, tsx, json, css]

  # Docs hooks (ultracite)
  - repo: local
    hooks:
      - id: ultracite-docs
        name: ultracite check (docs)
        entry: bun --cwd docs run check
        language: system
        pass_filenames: false
        files: ^docs/
        types: [file]
        types_or: [javascript, jsx, ts, tsx, json, css]

  # General hooks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
        args: [--maxkb=1000]
      - id: check-merge-conflict
