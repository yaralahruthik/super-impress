/**
 * Generated by orval v7.17.0 🍺
 * Do not edit manually.
 * Super Impress
 * OpenAPI spec version: 0.1.0
 */
import * as zod from 'zod';

/**
 * Create a new post for the authenticated user.
 * @summary Create Post Endpoint
 */
export const createPostBodyTitleMaxOne = 255;

export const createPostBody = zod
	.object({
		title: zod
			.union([zod.string().min(1).max(createPostBodyTitleMaxOne), zod.null()])
			.optional()
			.describe('Post title'),
		content: zod.string().min(1).describe('Post content'),
		tags: zod.array(zod.string()).optional().describe('Post tags'),
		status: zod
			.enum(['draft', 'published', 'archived', 'scheduled', 'failed'])
			.optional()
			.describe('Post status enumeration.'),
		scheduled_for: zod.union([zod.iso.datetime({}), zod.null()]).optional()
	})
	.describe('Schema for creating a new post.');

/**
 * List posts for the authenticated user with optional filters.
 * @summary List Posts Endpoint
 */
export const listPostsQueryLimitDefault = 100;
export const listPostsQueryLimitMax = 100;

export const listPostsQueryOffsetDefault = 0;
export const listPostsQueryOffsetMin = 0;

export const listPostsQueryParams = zod.object({
	status: zod
		.union([
			zod
				.enum(['draft', 'published', 'archived', 'scheduled', 'failed'])
				.describe('Post status enumeration.'),
			zod.null()
		])
		.optional()
		.describe('Filter by post status'),
	tag: zod.union([zod.string(), zod.null()]).optional().describe('Filter by tag'),
	limit: zod
		.number()
		.min(1)
		.max(listPostsQueryLimitMax)
		.default(listPostsQueryLimitDefault)
		.describe('Number of posts to return'),
	offset: zod.number().min(listPostsQueryOffsetMin).optional().describe('Number of posts to skip')
});

export const listPostsResponsePostsItemTitleMaxOne = 255;

export const listPostsResponse = zod
	.object({
		posts: zod.array(
			zod
				.object({
					title: zod
						.union([zod.string().min(1).max(listPostsResponsePostsItemTitleMaxOne), zod.null()])
						.optional()
						.describe('Post title'),
					content: zod.string().min(1).describe('Post content'),
					tags: zod.array(zod.string()).optional().describe('Post tags'),
					id: zod.number(),
					user_id: zod.number(),
					status: zod
						.enum(['draft', 'published', 'archived', 'scheduled', 'failed'])
						.describe('Post status enumeration.'),
					created_at: zod.iso.datetime({}),
					updated_at: zod.iso.datetime({}),
					scheduled_for: zod.union([zod.iso.datetime({}), zod.null()]),
					reason_failed: zod.union([zod.string(), zod.null()]),
					linkedin_post_id: zod.union([zod.string(), zod.null()])
				})
				.describe('Schema for post public data.')
		),
		total: zod.number()
	})
	.describe('Response schema for listing posts.');

/**
 * Get a specific post by ID.
 * @summary Get Post Endpoint
 */
export const getPostParams = zod.object({
	post_id: zod.number()
});

export const getPostResponseTitleMaxOne = 255;

export const getPostResponse = zod
	.object({
		title: zod
			.union([zod.string().min(1).max(getPostResponseTitleMaxOne), zod.null()])
			.optional()
			.describe('Post title'),
		content: zod.string().min(1).describe('Post content'),
		tags: zod.array(zod.string()).optional().describe('Post tags'),
		id: zod.number(),
		user_id: zod.number(),
		status: zod
			.enum(['draft', 'published', 'archived', 'scheduled', 'failed'])
			.describe('Post status enumeration.'),
		created_at: zod.iso.datetime({}),
		updated_at: zod.iso.datetime({}),
		scheduled_for: zod.union([zod.iso.datetime({}), zod.null()]),
		reason_failed: zod.union([zod.string(), zod.null()]),
		linkedin_post_id: zod.union([zod.string(), zod.null()])
	})
	.describe('Schema for post public data.');

/**
 * Update a specific post.
 * @summary Update Post Endpoint
 */
export const updatePostParams = zod.object({
	post_id: zod.number()
});

export const updatePostBodyTitleMaxOne = 255;

export const updatePostBody = zod
	.object({
		title: zod.union([zod.string().min(1).max(updatePostBodyTitleMaxOne), zod.null()]).optional(),
		content: zod.union([zod.string().min(1), zod.null()]).optional(),
		tags: zod.union([zod.array(zod.string()), zod.null()]).optional(),
		status: zod
			.union([
				zod
					.enum(['draft', 'published', 'archived', 'scheduled', 'failed'])
					.describe('Post status enumeration.'),
				zod.null()
			])
			.optional(),
		scheduled_for: zod.union([zod.iso.datetime({}), zod.null()]).optional()
	})
	.describe('Schema for updating a post (all fields optional).');

export const updatePostResponseTitleMaxOne = 255;

export const updatePostResponse = zod
	.object({
		title: zod
			.union([zod.string().min(1).max(updatePostResponseTitleMaxOne), zod.null()])
			.optional()
			.describe('Post title'),
		content: zod.string().min(1).describe('Post content'),
		tags: zod.array(zod.string()).optional().describe('Post tags'),
		id: zod.number(),
		user_id: zod.number(),
		status: zod
			.enum(['draft', 'published', 'archived', 'scheduled', 'failed'])
			.describe('Post status enumeration.'),
		created_at: zod.iso.datetime({}),
		updated_at: zod.iso.datetime({}),
		scheduled_for: zod.union([zod.iso.datetime({}), zod.null()]),
		reason_failed: zod.union([zod.string(), zod.null()]),
		linkedin_post_id: zod.union([zod.string(), zod.null()])
	})
	.describe('Schema for post public data.');

/**
 * Delete a specific post.
 * @summary Delete Post Endpoint
 */
export const deletePostParams = zod.object({
	post_id: zod.number()
});

/**
 * Schedule a post for future publishing.
 * @summary Schedule Post Endpoint
 */
export const schedulePostParams = zod.object({
	post_id: zod.number()
});

export const schedulePostBody = zod
	.object({
		scheduled_for: zod.iso.datetime({}).describe('When to publish (UTC)')
	})
	.describe('Request to schedule a post.');

export const schedulePostResponseTitleMaxOne = 255;

export const schedulePostResponse = zod
	.object({
		title: zod
			.union([zod.string().min(1).max(schedulePostResponseTitleMaxOne), zod.null()])
			.optional()
			.describe('Post title'),
		content: zod.string().min(1).describe('Post content'),
		tags: zod.array(zod.string()).optional().describe('Post tags'),
		id: zod.number(),
		user_id: zod.number(),
		status: zod
			.enum(['draft', 'published', 'archived', 'scheduled', 'failed'])
			.describe('Post status enumeration.'),
		created_at: zod.iso.datetime({}),
		updated_at: zod.iso.datetime({}),
		scheduled_for: zod.union([zod.iso.datetime({}), zod.null()]),
		reason_failed: zod.union([zod.string(), zod.null()]),
		linkedin_post_id: zod.union([zod.string(), zod.null()])
	})
	.describe('Schema for post public data.');

/**
 * Cancel a scheduled post.
 * @summary Cancel Schedule Endpoint
 */
export const cancelScheduleParams = zod.object({
	post_id: zod.number()
});

export const cancelScheduleResponseTitleMaxOne = 255;

export const cancelScheduleResponse = zod
	.object({
		title: zod
			.union([zod.string().min(1).max(cancelScheduleResponseTitleMaxOne), zod.null()])
			.optional()
			.describe('Post title'),
		content: zod.string().min(1).describe('Post content'),
		tags: zod.array(zod.string()).optional().describe('Post tags'),
		id: zod.number(),
		user_id: zod.number(),
		status: zod
			.enum(['draft', 'published', 'archived', 'scheduled', 'failed'])
			.describe('Post status enumeration.'),
		created_at: zod.iso.datetime({}),
		updated_at: zod.iso.datetime({}),
		scheduled_for: zod.union([zod.iso.datetime({}), zod.null()]),
		reason_failed: zod.union([zod.string(), zod.null()]),
		linkedin_post_id: zod.union([zod.string(), zod.null()])
	})
	.describe('Schema for post public data.');

/**
 * Reschedule a post.
 * @summary Reschedule Post Endpoint
 */
export const reschedulePostParams = zod.object({
	post_id: zod.number()
});

export const reschedulePostBody = zod
	.object({
		scheduled_for: zod.iso.datetime({}).describe('When to publish (UTC)')
	})
	.describe('Request to schedule a post.');

export const reschedulePostResponseTitleMaxOne = 255;

export const reschedulePostResponse = zod
	.object({
		title: zod
			.union([zod.string().min(1).max(reschedulePostResponseTitleMaxOne), zod.null()])
			.optional()
			.describe('Post title'),
		content: zod.string().min(1).describe('Post content'),
		tags: zod.array(zod.string()).optional().describe('Post tags'),
		id: zod.number(),
		user_id: zod.number(),
		status: zod
			.enum(['draft', 'published', 'archived', 'scheduled', 'failed'])
			.describe('Post status enumeration.'),
		created_at: zod.iso.datetime({}),
		updated_at: zod.iso.datetime({}),
		scheduled_for: zod.union([zod.iso.datetime({}), zod.null()]),
		reason_failed: zod.union([zod.string(), zod.null()]),
		linkedin_post_id: zod.union([zod.string(), zod.null()])
	})
	.describe('Schema for post public data.');
